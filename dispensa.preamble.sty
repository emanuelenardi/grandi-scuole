\usepackage{comment}
\usepackage{xparse}

% NOTE definizione di nomi di linugaggi di programmazione
\NewDocumentCommand{\Cpp}{}{C\texttt{++}}
\NewDocumentCommand{\Java}{}{\textbf{Java}}
\NewDocumentCommand{\Javalogo}{}{%
	\raisebox{-4pt}{%
		\includegraphics[width=.10\textwidth]{java-logo.png}%
	}%
}

\usepackage{microtype}
\usepackage[main=italian, english]{babel}
\usepackage[useregional]{datetime2}

% NOTE per i listati di codice
\usepackage[newfloat, cachedir=_minted-cache]{minted}
% \usemintedstyle{manni}

% NOTE opzioni globali per il pacchetto minted
\setminted{
	,texcomments
	,mathescape
	,breaklines  = true
	,frame       = none
	,fontsize = \footnotesize
	,fontseries  = bold
	,numberblanklines = false
}

% NOTE opzioni per l'ambiente del linguaggio C++
\setminted[cpp]{
	,linenos = true
	,tabsize = 4
}

% NOTE opzioni per l'ambiente del linguaggio bash
\setminted[bash]{
	,linenos = true
	,tabsize = 4
}

% NOTE opzioni per in codice in linea dei linguaggi
\newmintinline[cpp]{cpp}{escapeinside = ||}
\newmintinline[bash]{bash}{escapeinside = ||}

\usepackage{graphicx}
\graphicspath{%
	{./assets/images/}%
}

\usepackage{array, booktabs}

% NOTE toglie l'intentazione a tutto il documento
% \setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}

\usepackage{mathtools}
\usepackage{tikz}
\usetikzlibrary{tikzmark,calc,fit}

% NOTE The cmap package is intended to make the PDF files generated by pdflatex
% 'searchable and copyable' in acrobat reader and other compliant PDF viewers.
\usepackage{cmap}

\usepackage[
	,a4paper
	,top	= 1in
	,bottom	= 1in
	,left	= 1.25in
	,right	= 1.25in
	,heightrounded
]{geometry}
